# WiFi (ESP32 native driver)
CONFIG_WIFI_ESP32=y
CONFIG_ESP32_WIFI_STATIC_RX_BUFFER_NUM=10
CONFIG_ESP32_WIFI_DYNAMIC_RX_BUFFER_NUM=32
CONFIG_ESP32_WIFI_DYNAMIC_TX_BUFFER_NUM=32

# ESP32 needs more stack for WiFi operations
CONFIG_MAIN_STACK_SIZE=5200
CONFIG_SHELL_STACK_SIZE=5200

# Heap: WiFi driver needs ~51K, extra for runtime TX buffer allocs
CONFIG_HEAP_MEM_POOL_SIZE=81920

# Net buffers: more to sustain throughput
CONFIG_NET_BUF_TX_COUNT=40
CONFIG_NET_BUF_RX_COUNT=32

# TCP windows sized to fit within buffer pool
CONFIG_NET_TCP_MAX_SEND_WINDOW_SIZE=16384
CONFIG_NET_TCP_MAX_RECV_WINDOW_SIZE=16384

# eventfd slots for HTTP server
CONFIG_ZVFS_EVENTFD_MAX=4
